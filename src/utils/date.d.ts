/**
 * Formats a date string into a readable format (e.g., "Jan 15, 2024")
 * @param dateString - A date string, Date object, or null/undefined
 * @returns A formatted date string, or an empty string if dateString is falsy
 */
export declare function formatDate(dateString: string | Date | null | undefined): string; 